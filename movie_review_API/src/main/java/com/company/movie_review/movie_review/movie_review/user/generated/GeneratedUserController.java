package com.company.movie_review.movie_review.movie_review.user.generated;

import com.company.movie_review.movie_review.movie_review.user.User;
import com.company.movie_review.movie_review.movie_review.user.UserManager;
import com.company.movie_review.movie_review.movie_review.user.generated.GeneratedUser.Identifier;
import com.speedment.common.annotation.GeneratedCode;
import com.speedment.common.json.Json;
import com.speedment.enterprise.plugins.json.JsonCollectors;
import com.speedment.enterprise.plugins.json.JsonComponent;
import com.speedment.enterprise.plugins.json.JsonEncoder;
import com.speedment.enterprise.plugins.spring.runtime.AbstractFilter;
import com.speedment.enterprise.plugins.spring.runtime.AbstractSort;
import com.speedment.enterprise.plugins.spring.runtime.ControllerUtil;
import com.speedment.runtime.field.Field;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.CrossOrigin;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestParam;

import java.util.Comparator;
import java.util.EnumSet;
import java.util.List;
import java.util.Optional;
import java.util.Set;
import java.util.function.Predicate;
import java.util.stream.Stream;
import javax.annotation.PostConstruct;

import static java.util.stream.Collectors.toList;

/**
 * The default REST controller logic for User entities.
 * <p>
 * This file has been automatically generated by Speedment. Any changes made to
 * it will be overwritten.
 * 
 * @author Speedment
 */
@GeneratedCode("Speedment")
@CrossOrigin(origins = "*", maxAge = 3600)
public abstract class GeneratedUserController {
    
    protected @Autowired JsonComponent jsonComponent;
    protected @Autowired UserManager manager;
    protected JsonEncoder<User> encoder;
    
    @PostConstruct
    void createUserEncoder() {
        encoder = jsonComponent.<User>emptyEncoder()
            .put("id", User.ID)
            .put("username", User.USERNAME)
            .put("password", User.PASSWORD)
            .put("email", User.EMAIL)
            .put("phone", User.PHONE)
            .put("fullname", User.FULLNAME)
            .put("address", User.ADDRESS)
            .build();
    }
    
    @GetMapping(path = "/user", produces = "application/json")
    public String get(
            @RequestParam(name = "filter", defaultValue = "[]") String filters,
            @RequestParam(name = "sort", defaultValue = "[]") String sorters,
            @RequestParam(value = "start", defaultValue = "0") long start,
            @RequestParam(value = "limit", defaultValue = "25") long limit) {
        
        return getHelper(
            ControllerUtil.parseFilters(filters, UserFilter::new).collect(toList()),
            ControllerUtil.parseSorts(sorters, UserSort::new).collect(toList()),
            start,
            limit
        );
    }
    
    protected final Set<Identifier> parseColumns(String jsonColumnList) {
        try {
            @SuppressWarnings("unchecked")
            final List<String> parsed = (List<String>) Json.fromJson(jsonColumnList);
            final Set<GeneratedUser.Identifier> result = EnumSet.noneOf(GeneratedUser.Identifier.class);
            parsed.stream().map(this::parseColumn).forEach(result::add);
            return result;
        } catch (final ClassCastException ex) {
            throw new IllegalArgumentException("Error in parsed JSON.");
        }
    }
    
    protected final Identifier parseColumn(String jsonColumn) {
        switch (jsonColumn) {
            case "id":       return GeneratedUser.Identifier.ID;
            case "username": return GeneratedUser.Identifier.USERNAME;
            case "password": return GeneratedUser.Identifier.PASSWORD;
            case "email":    return GeneratedUser.Identifier.EMAIL;
            case "phone":    return GeneratedUser.Identifier.PHONE;
            case "fullname": return GeneratedUser.Identifier.FULLNAME;
            case "address":  return GeneratedUser.Identifier.ADDRESS;
            default: throw new IllegalArgumentException(
                "Unknown column '" + jsonColumn + "'."
            );
        }
    }
    
    protected final Field<User> fieldOf(Identifier columnId) {
        switch (columnId) {
            case ID:       return User.ID;
            case USERNAME: return User.USERNAME;
            case PASSWORD: return User.PASSWORD;
            case EMAIL:    return User.EMAIL;
            case PHONE:    return User.PHONE;
            case FULLNAME: return User.FULLNAME;
            case ADDRESS:  return User.ADDRESS;
            default: throw new IllegalArgumentException(
                "Unknown column '" + columnId + "'."
            );
        }
    }
    
    protected String getHelper(
            List<Predicate<User>> predicates,
            List<Comparator<User>> sorters,
            long start,
            long limit) {
        Stream<User> stream = manager.stream();
        
        for (final Predicate<User> predicate : predicates) {
            stream = stream.filter(predicate);
        }
        
        if (!sorters.isEmpty()) {
            final Optional<Comparator<User>> comparator = sorters.stream()
                .reduce(Comparator::thenComparing);
            
            stream = stream.sorted(comparator.get());
        }
        
        return stream
            .skip(start)
            .limit(limit)
            .collect(JsonCollectors.toList(encoder));
    }
    
    /**
     * How to filter the results from the controller. This class is designed to
     * be deserialized automatically from JSON.
     */
    public final static class UserFilter extends AbstractFilter<User> {
        
        public UserFilter(
                String operator,
                String property,
                String value) {
            super(operator, property, value);
        }
        
        @Override
        public Predicate<User> toPredicate() {
            switch (property()) {
                case "id" : {
                    final int v = Integer.parseInt(value());
                    switch (operator()) {
                        case "eq"   : return User.ID.equal(v);
                        case "ne"   : return User.ID.notEqual(v);
                        case "lt"   : return User.ID.lessThan(v);
                        case "le"   : return User.ID.lessOrEqual(v);
                        case "gt"   : return User.ID.greaterThan(v);
                        case "ge"   : return User.ID.greaterOrEqual(v);
                        case "like" : // Fallthrough
                        default : throw new IllegalArgumentException(
                            "'" + operator() + "' is not a valid operator for " +
                            "User.id."
                        );
                    }
                }
                case "username" : {
                    final String v = value();
                    switch (operator()) {
                        case "eq"   : return User.USERNAME.equal(v);
                        case "ne"   : return User.USERNAME.notEqual(v);
                        case "lt"   : return User.USERNAME.lessThan(v);
                        case "le"   : return User.USERNAME.lessOrEqual(v);
                        case "gt"   : return User.USERNAME.greaterThan(v);
                        case "ge"   : return User.USERNAME.greaterOrEqual(v);
                        case "like" : return User.USERNAME.contains(v);
                        default : throw new IllegalArgumentException(
                            "'" + operator() + "' is not a valid operator for " +
                            "User.username."
                        );
                    }
                }
                case "password" : {
                    final String v = value();
                    switch (operator()) {
                        case "eq"   : return User.PASSWORD.equal(v);
                        case "ne"   : return User.PASSWORD.notEqual(v);
                        case "lt"   : return User.PASSWORD.lessThan(v);
                        case "le"   : return User.PASSWORD.lessOrEqual(v);
                        case "gt"   : return User.PASSWORD.greaterThan(v);
                        case "ge"   : return User.PASSWORD.greaterOrEqual(v);
                        case "like" : return User.PASSWORD.contains(v);
                        default : throw new IllegalArgumentException(
                            "'" + operator() + "' is not a valid operator for " +
                            "User.password."
                        );
                    }
                }
                case "email" : {
                    final String v = value();
                    switch (operator()) {
                        case "eq"   : return User.EMAIL.equal(v);
                        case "ne"   : return User.EMAIL.notEqual(v);
                        case "lt"   : return User.EMAIL.lessThan(v);
                        case "le"   : return User.EMAIL.lessOrEqual(v);
                        case "gt"   : return User.EMAIL.greaterThan(v);
                        case "ge"   : return User.EMAIL.greaterOrEqual(v);
                        case "like" : return User.EMAIL.contains(v);
                        default : throw new IllegalArgumentException(
                            "'" + operator() + "' is not a valid operator for " +
                            "User.email."
                        );
                    }
                }
                case "phone" : {
                    final String v = value();
                    switch (operator()) {
                        case "eq"   : return User.PHONE.equal(v);
                        case "ne"   : return User.PHONE.notEqual(v);
                        case "lt"   : return User.PHONE.lessThan(v);
                        case "le"   : return User.PHONE.lessOrEqual(v);
                        case "gt"   : return User.PHONE.greaterThan(v);
                        case "ge"   : return User.PHONE.greaterOrEqual(v);
                        case "like" : return User.PHONE.contains(v);
                        default : throw new IllegalArgumentException(
                            "'" + operator() + "' is not a valid operator for " +
                            "User.phone."
                        );
                    }
                }
                case "fullname" : {
                    final String v = value();
                    switch (operator()) {
                        case "eq"   : return User.FULLNAME.equal(v);
                        case "ne"   : return User.FULLNAME.notEqual(v);
                        case "lt"   : return User.FULLNAME.lessThan(v);
                        case "le"   : return User.FULLNAME.lessOrEqual(v);
                        case "gt"   : return User.FULLNAME.greaterThan(v);
                        case "ge"   : return User.FULLNAME.greaterOrEqual(v);
                        case "like" : return User.FULLNAME.contains(v);
                        default : throw new IllegalArgumentException(
                            "'" + operator() + "' is not a valid operator for " +
                            "User.fullname."
                        );
                    }
                }
                case "address" : {
                    final String v = value();
                    switch (operator()) {
                        case "eq"   : return User.ADDRESS.equal(v);
                        case "ne"   : return User.ADDRESS.notEqual(v);
                        case "lt"   : return User.ADDRESS.lessThan(v);
                        case "le"   : return User.ADDRESS.lessOrEqual(v);
                        case "gt"   : return User.ADDRESS.greaterThan(v);
                        case "ge"   : return User.ADDRESS.greaterOrEqual(v);
                        case "like" : return User.ADDRESS.contains(v);
                        default : throw new IllegalArgumentException(
                            "'" + operator() + "' is not a valid operator for " +
                            "User.address."
                        );
                    }
                }
                default : throw new IllegalArgumentException(
                    "'" + property() + "' is not a valid User property."
                );
            }
        }
    }
    
    /**
     * How to sort the results from the controller. This class is designed to be
     * deserialized automatically from JSON.
     */
    public final static class UserSort extends AbstractSort<User> {
        
        public UserSort(String property, String direction) {
            super(property, direction);
        }
        
        @Override
        public Comparator<User> toComparator() {
            final Comparator<User> comparator;
            switch (property()) {
                case "id"       : comparator = User.ID.comparator();       break;
                case "username" : comparator = User.USERNAME.comparator(); break;
                case "password" : comparator = User.PASSWORD.comparator(); break;
                case "email"    : comparator = User.EMAIL.comparator();    break;
                case "phone"    : comparator = User.PHONE.comparator();    break;
                case "fullname" : comparator = User.FULLNAME.comparator(); break;
                case "address"  : comparator = User.ADDRESS.comparator();  break;
                default : throw new IllegalArgumentException(
                    "'" + property() + "' is not a valid User property."
                );
            }
            
            switch (direction()) {
                case "ASC"  : return comparator;
                case "DESC" : return comparator.reversed();
                default : throw new IllegalArgumentException(
                    "'" + direction() + "' is not a valid sort direction. " +
                    "Use either 'ASC' or 'DESC', or leave out."
                );
            }
        }
    }
}